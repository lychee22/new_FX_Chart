# 前端规范

> 写给ai规范

## 

## 核心总则（最高优先级）

**1. 能不改就不改，能少读就少读，能局部绝不全局**

**2. 所有文件读取、代码分析、修改行为必须最小范围**

**3. 禁止主动扩写上下文、禁止自动扫描全项目**

**4.如果修改无法解决问题，直接重写方法。**

以下目录**永远不读取、不遍历、不分析**（节省巨量 Token）：

- dist / build / out / target
- node_modules/
- .git / .vscode / .idea
- 静态资源、图片、svg、配置无关文件
- 老旧废弃代码目录（sapdata 历史旧代码）



## 本项目专属特殊约束（适配你的外汇图表重构项目）

SMA / EMA / Bollinger / SAR / Ichimoku 等指标原有逻辑禁止乱改，只新增、只修 Bug，不重构整套算法。

\1. 先确认**最小需要读取的文件**（只读取前端相关）

\5. 不做多余优化、多余解释

- 自动读取无关多个文件
- 长篇无用铺垫、冗余解释

## 前端的项目结构

读取"C:\D\projects\FORFX_CHART\README.md"

只读5.2前端结构 5.3、5.4章节



## 工作流程规范

1. 先确认**最小需要读取的文件**
2. 只读必要内容，不扩范围
3. 只修改问题代码块
4. 局部输出，不整文件刷屏
5. 不做多余优化、多余解释
6. 编写代码时遵循一个功能一个方法，严格执行react、js最佳实践



## 违规判定（AI 绝对禁止行为）

- 出现以下行为属于违规、大幅浪费 Token：

  - 自动遍历整个项目目录
  - 自动读取无关多个文件
  - 重复读取已读取上下文
  - 主动全局格式化、重构、润色
  - 长篇无用铺垫、冗余解释

  

  

## 版本控制

git指令提交范围：（`C:\D\projects\FORFX_CHART` 上层目录）



## 生效说明

​	本文件为 **最高优先级强制规则**，后续所有 AI 编码工作永久生效，任何指令默认服从本规范。
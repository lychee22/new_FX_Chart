# 一目均衡图与旧 chart 对齐记录

更新时间：2026-07-21 17:32:27

## 处理范围

本次只修改新项目的“一目均衡图（Ichimoku）”，其他叠加指标和副图指标不调整。

## 原因分析

旧 `chart` 项目除了绘制五条指标线，还会在先行跨度 A（Senkou A）与先行跨度 B（Senkou B）之间绘制绿色网格云层。新项目原先把五组结果全部按普通折线处理，因此即使存在两条云边界，也不会形成绿色云层。

另外，新项目原计算没有完整复刻旧项目行为：

- 日、周、月周期没有使用旧项目的 `vap`、`vam`、`mp` 模态字段。
- 先行跨度 A、先行跨度 B 和蓝线没有统一向未来前移 22 根。
- Ichimoku 请求没有携带未来空白时间点，前移后的数据没有绘图空间。
- 前端没有旧项目的红色/蓝色交叉强度信号。

## 修改方案

- 后端保留默认参数 `[7, 22, 44]`，按周期选择模态字段或 OHLC 字段。
- Tenkan、Kijun 保留在当前时间；Senkou A、Senkou B、Chikou 按旧项目行为向未来移动 22 根。
- 仅 Ichimoku 请求 `shift=true`，其他指标继续使用原请求方式。
- 前端增加 Ichimoku 专用 primitive，在 Senkou A/B 之间绘制旧项目绿色网格云层。
- 恢复 Tenkan/Kijun 交叉产生的红色向上、蓝色向下 1 至 3 级三角信号。
- 指标切换时卸载旧 primitive，并通过请求序号避免异步旧结果覆盖新选择。

## 验证结果

- `mvn test`：通过，2 个 Ichimoku 回归用例，0 失败、0 错误。
- `npm run build`：通过，TypeScript 检查与 Vite 生产构建成功。
- 接口验证：Ichimoku 为 350 个时间槽，Senkou A/B 与 Chikou 可进入未来区域；SMA 仍为 300 个时间槽。
- 自动浏览器视觉验收：Codex 内置浏览器受本地 URL 安全策略限制，无法打开 `http://127.0.0.1:5173/`，未绕过该限制。

## 人工视觉验收点

1. 打开新项目，选择“一目均衡图”。
2. 确认两条绿色边界之间出现浅绿/深绿网格云层。
3. 确认云层延伸至最后一根有效 K 线右侧的未来区域。
4. 确认灰、粉、两条绿、蓝五条线仍正常显示。
5. 切换到其他指标，确认绿色云层和红蓝信号立即移除，其他指标显示不变。

## 运行记录

- 2026-07-21 17:36：确认 8080 端口由本项目先前启动的 `java.exe`（PID 38684）占用；按用户要求终止该进程并验证端口已释放，未重新启动后端。

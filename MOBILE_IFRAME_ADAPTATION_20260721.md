# 手机竖屏与 iframe 适配记录

更新时间：2026-07-21 18:32:39

## 已确认需求

- 新前端后续通过 `iframe` 接入。
- `iframe` 可能占满手机屏幕，也可能只是宿主页面中的固定区域。
- 页面在 `iframe` 内部自行上下滚动，不向宿主发送高度，也不自动撑开 `iframe`。
- 顶部工具栏尽量保持一行，通过左右滑动查看隐藏控件。
- 图表内左右拖动查看历史 K 线，页面上下滑动查看主图和已选副图。
- 每个已选副图保留独立、清晰的高度，不压缩到同一屏。
- 主要适配常见 iPhone Safari、安卓 Chrome 及对应 WebView。

## 实现内容

### iframe 内部滚动

- 手机宽度下解除 `.app` 的 `100vh + overflow:hidden` 限制。
- 使用 `100dvh`，并保留 `100vh` 回退，兼容较旧的 iOS/Android 浏览器。
- 页面只允许纵向内容滚动，不产生整页横向溢出。
- 使用 `overscroll-behavior` 减少 iframe 滚动到边缘后传递给宿主页面的情况。

### 工具栏

- `768px` 及以下保持单行，工具栏自身支持横向滑动。
- 下拉框、按钮和语言按钮高度统一为 `44px`。
- 下拉框字号提升到 `16px`，避免 iOS Safari 聚焦时自动放大页面。
- 副图多选菜单在手机端改为视口浮层，避免被横向滚动工具栏裁剪。
- 外部点击监听改为 Pointer Event，并补充按钮类型和 ARIA 属性。

### 图表与副图

- 主图移动端基础高度为 `max(420px, 62vh)`；支持动态视口的浏览器使用 `62dvh`。
- 每选择一个副图，图表总高度增加 `220px`。
- 手机端主图 pane 权重为副图的两倍；每个副图保持相同独立权重。
- 桌面端继续使用原有等比例 pane 布局。
- 使用 `ResizeObserver` 监听 iframe 或容器尺寸变化并及时调整画布。

### 触摸手势

- 手机宽度下关闭图表的纵向触摸拖动和鼠标滚轮捕获，纵向操作交给 iframe 页面。
- 图表继续处理横向触摸拖动，保留历史 K 线移动和图表缩放能力。
- 容器使用 `touch-action: pan-y`，降低图表横向拖动与页面纵向滚动之间的冲突。

### iPhone 安全区域

- viewport 增加 `viewport-fit=cover`。
- 工具栏、标题、菜单、信息浮层和底部间距使用 `env(safe-area-inset-*)`。

## 验证结果

- `npm run build`：通过。
- TypeScript 检查：通过。
- Vite 生产构建：通过，共转换 98 个模块。
- 静态断点检查：`320px`、`375px`、`390px`、`430px`、`768px` 进入手机布局，`1024px` 保持桌面布局。
- 副图高度检查：1/2/4 个副图分别增加 220/440/880px。
- 本次没有修改后端、指标计算或接口契约。

## 人工验收建议

1. 在固定高度 iframe 中上下滑动，确认可以从主图滚动到所有副图。
2. 在图表中左右拖动，确认历史 K 线可以移动，同时上下滑动不会被图表拦截。
3. 横向滑动顶部工具栏，确认所有选择器、按钮和语言切换均可操作。
4. 打开副图多选菜单，确认菜单没有被工具栏裁剪。
5. 分别选择 1 个、2 个和 4 个副图，确认页面高度递增且每个副图可读。
6. 在全屏 iframe 和局部高度 iframe 中各验证一次。

受当前 Codex 本地 URL 浏览器安全策略限制，本次无法执行真实 Safari/Chrome 设备截图验收；已完成编译、构建和静态响应式检查。
